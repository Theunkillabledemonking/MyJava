import java.util.Scanner;

public class nado {
  public static void main(String[] args) {
    // String.format("%.2f") 소수점 두자리 수까지 표현
    // 시프트되는 비트 수가 피연산의 크기를 넘는 경우 해당 숫자의 최하위 5비트만 사용
    // System.out.println((num > 0) ? "양수" :(num < 0) ? "음수" : "0"); 3항연산자
  }

}
